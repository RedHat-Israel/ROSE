RaananaTiraProject
==================

Creating a tarball
------------------

    python setup.py sdist

Running the game
----------------

Start up to 4 clients:

    ./rtp-client player-name drive-module

Start the server on some machine:

    ./rtp-server

For drive modules, see the examples direcotory.

Notes
-----

Curently it configure to localhost. To use a server on another machine, edit
rtp/common/config.py and set the host option.
